# Order file limit updated to 20 MB

Customer/admin order-creation file uploads now allow up to 20 MB per file.
Payment receipt limits were intentionally left unchanged.
Supabase Storage bucket setup now uses/updates a 20 MB bucket limit when the existing bucket is encountered.
